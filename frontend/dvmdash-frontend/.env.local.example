# Frontend environment variables for local development outside Docker

# API URL for client-side requests (browser)
# This should point to where your API is accessible from the browser
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: API URL for server-side rendering
# Only needed if different from the client-side URL
# NEXT_PUBLIC_METADATA_API_URL=http://api:8000

# Optional: Enable API proxy (if you need to proxy requests through Next.js)
# NEXT_PUBLIC_USE_API_PROXY=false
